# Remove cached env, venv, __pycache__, etc.
git rm -r --cached env venv .venv __pycache__ faiss_index liquide

# Stage updated .gitignore
git add .gitignore

# Commit the cleanup
git commit -m "remove tracked env and cache files"

# Push again
git push origin main
liquide/
liquide/
liquide/lib/
